<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gridentertainment.net.projectpickleseller">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.MainActivity" />
        <activity android:name=".UI.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.SignUpActivity" />
        <activity android:name=".UI.ResetActivity" />
        <activity android:name=".UI.AddItemActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".UI.ViewOrderActivity" />
        <activity android:name=".UI.PhoneLoginActivity" />
        <activity android:name=".UI.PhoneVerifyActivity" />
        <activity android:name=".UI.DeliveryInfoActivity"></activity>
    </application>

</manifest>